spring:
  datasource:
    url: jdbc:h2:file:./db
    username: u
    password: p
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create
  h2:
    console:
      enabled: true